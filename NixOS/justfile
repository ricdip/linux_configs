# Rebuild system
rebuild:
    sudo nixos-rebuild switch

# Rebuild specific hostname
rebuild-flake FLAKE:
    sudo nixos-rebuild switch --flake /etc/nixos#{{FLAKE}}

# List system generations
history:
    sudo nix-env --list-generations --profile /nix/var/nix/profiles/system